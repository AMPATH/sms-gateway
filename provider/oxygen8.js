var obj={
  sendSMS: function(messageObj,cb){
    // console.log(messageObj);
    // parse messageObj for each phonenumber
    // send request to the service provider for each phonenumber
    // get the return status of the message as sent/failed at this point of time
    // if the id is generating by the provider , then assign that id back to the messageObj
    // save messageObj

  }
};

module.exports=obj;
